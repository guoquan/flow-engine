name: Deploy to GitHub Pages

on:
  push:
    branches: ["main", "track/*"]
  workflow_dispatch:

permissions:
  contents: write # Need write for potentially cleaning up or handling versions
  pages: write
  id-token: write

concurrency:
  group: "pages-${{ github.ref_name }}" # Separate concurrency per branch
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 9
          run_install: false

      - name: Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup pnpm cache
        uses: actions/cache@v4
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Install Dependencies
        run: pnpm install

      - name: Run Tests
        run: pnpm test

      - name: Calculate Deployment Path
        id: path
        run: |
          if [ "${{ github.ref_name }}" == "main" ]; then
            echo "base=/flow-engine/" >> $GITHUB_OUTPUT
            echo "out=dist-demo" >> $GITHUB_OUTPUT
          else
            # For track/v0.2, base becomes /flow-engine/track/v0.2/
            echo "base=/flow-engine/${{ github.ref_name }}/" >> $GITHUB_OUTPUT
            echo "out=dist-demo/${{ github.ref_name }}" >> $GITHUB_OUTPUT
          fi

      - name: Build Demo
        run: |
          mkdir -p ${{ steps.path.outputs.out }}
          GITHUB_PAGES=true VITE_BASE=${{ steps.path.outputs.base }} OUT_DIR=${{ steps.path.outputs.out }} pnpm run build:demo

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # We upload the root of dist-demo so that all versions are included
          # Note: GitHub Actions upload-pages-artifact only supports a single folder.
          # To support multiple tracks simultaneously on one site, we'd need a more complex
          # strategy (like an orphan 'gh-pages' branch). 
          # For now, we deploy the specific track to its subfolder.
          path: './dist-demo'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4